bindPort = 7000
    vhostHTTPPort = 8080
    webServer.addr = "0.0.0.0"
    webServer.port = 7500
    webServer.user = "admin"
    webServer.password = "admin123"
    auth.token = "frp-auth-token-change-in-production"
    subDomainHost = "nas.yourdomain.com"
    log.to = "./frps.log"
    log.level = "info"
    log.maxDays = 3
    